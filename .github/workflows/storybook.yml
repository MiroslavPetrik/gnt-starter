name: 🎨 Storybook

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  storybook:
    name: 🎨 Storybook Build & Deploy to GH Pages
    permissions:
      pages: write # to deploy to Pages
      id-token: write # to verify the deployment originates from an appropriate source

    runs-on: ubuntu-latest

    steps:
      - name: Use variables
        run: |
          echo "instance : $GEL_INSTANCE"
        env:
          GEL_INSTANCE: ${{ vars.GEL_INSTANCE }}

      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/Iron

      - name: Cache node_modules
        id: cache-node-modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-modules-${{ hashFiles('**/package-lock.json') }}

      - name: Install
        run: npm install

      - name: Generate Gel JS client
        run: npm run db:gel-edgeql-js
        env:
          GEL_SECRET_KEY: ${{ secrets.GEL_SECRET_KEY }}
          GEL_INSTANCE: ${{ vars.GEL_INSTANCE }}

      - name: Build Storybook
        run: SKIP_ENV_VALIDATION=true npm run storybook:build

      - name: Upload Storybook
        uses: actions/upload-pages-artifact@v4
        with:
          path: storybook-static

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          token: ${{ github.token }}
